import React from "react"
import Typist from "react-typist"

interface IProps {
  textList: string[]
}

const ReactComponent: React.FC<IProps> = ({ textList }) => {
  /*textList
      .slice(0, textList.length - 1)
      .map((text: string) => <div>{text}</div>)*/
  return <></>
}

export default ReactComponent
